# Terminal Velocity game host

UrsaMU + `@ursamu/sprawl-plugin` (vendored). Client is separate (Vercel).

- Config: `config/config.json` (from sample)
- Theme: `theme/installed/termv`
- Do not commit live `data/`, `wiki/`, or secrets
