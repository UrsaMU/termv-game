# Game help

Sprawl command help ships inside `@ursamu/sprawl-plugin`
(`packages/sprawl/help/`). Drop extra game-specific `.md`
files here if needed; they are picked up when the help
plugin scans the game help dir (if configured).
